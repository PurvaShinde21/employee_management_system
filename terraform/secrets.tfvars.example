# ============================================================
# Terraform Secrets — EXAMPLE FILE
# Copy this to secrets.tfvars, fill in your values, then run:
#   terraform apply -var-file="secrets.tfvars"
#
# secrets.tfvars is gitignored (*.tfvars). NEVER commit it.
# ============================================================

db_username = "your_db_admin_username"
db_password = "your_strong_db_password_here"
